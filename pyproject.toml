[project]
name = "mercado-livre-analytics"
version = "0.1.0"
description = "MCP server para analytics do Mercado Livre"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]
